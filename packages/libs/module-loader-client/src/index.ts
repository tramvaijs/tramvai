import { addLink } from './addLink';
import { addScript } from './addScript';

export { loadModule, waitModule } from './module';
export * from './defaults';
export * from './types.h';

export { addLink, addScript };
