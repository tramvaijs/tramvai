import type { SimplifiedPWAConfig } from '@tramvai/plugin-base-builder/lib/types';

export const pwaConfigs = [] as SimplifiedPWAConfig[];
export const workboxEnabled = false;
export const manifestEnabled = false;
export const pwaScopes = [] as string[];
