/**
 * @jest-environment @tramvai/test-unit-jest/lib/jsdom-environment
 */
import React from 'react';
import { testComponent } from '@tramvai/test-react';
import { testChildApp } from '@tramvai/test-child-app';
import childApp from './index';

describe('testChildApp', () => {
  it('base test', async () => {
    const {
      childApp: { di, Component },
      close,
    } = await testChildApp(childApp);
    const props = {};
    const { render } = testComponent(<Component di={di} props={props} />);

    expect(render.getAllByText('Child App microfront')).toBeTruthy();

    return close();
  });
});
