import { expect } from '@playwright/test';
import { test } from '~test/base-test';

test('should navigate to main page', async ({ application, page }) => {
  await application.goto((pages) => pages.main);

  expect(await page.content()).toContain('Welcome!');
});
